import React from 'react';
import CartCard from './CartCard';

const Cart = (props) => {
    return (
        <div className='cart'>
            <CartCard />
        </div>
    );
}

export default Cart;